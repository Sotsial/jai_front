import { Button } from "antd";

const FilterMobile = () => {
  return (
    <div>
      <Button
        block
        size="large"
        style={{ backgroundColor: "#1c1819", color: "#fff" }}
      >
        Фильтр
      </Button>
    </div>
  );
};

export default FilterMobile;
